// import { Adjust, AdjustEvent } from '@adjustcom/adjust-web-sdk';

export const AdjustPurchaseEvent = (amount: number, currency: string, transactionId: string) => {
    // const adjustEvent = new AdjustEvent(AdjustEvents.purchase);
    // adjustEvent.setRevenue(amount, currency);
    // adjustEvent.setTransactionId(transactionId);
    // Adjust.trackEvent(adjustEvent);
    // Logger.log('Sent Adjust event: ' + JSON.stringify(adjustEvent));
};

//TODO: Single-OptIn Event

//TODO: Double-OptIn Event
